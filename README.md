# Instruction

## Installation

`bundle install`

`yarn install`

## Create db, seed the data and fetch prices

`rails db:reset`

`rails fetch_prices`

## Run

`rails s`

## Seed dump

`rails db:seed:dump`
